import { Component, Inject } from "@angular/core";
import { ActivatedRoute } from "@angular/router";
//import { StaticModel } from "../../../repo/repository-static";
//import { Product } from "../../../repo/repo.model";

//import { rootReducer, actions, CostService, Product } from "./../ngrx/index";


@Component({
  selector: "cost-table",
  templateUrl: "cost-table.component.html"
})
export class CostTableComponent {
  //constructor(private model: CostService) { }

  //getProducts(): Product[] {
  //  return this.model.Products;
  //}

  //selectedProduct: string;

  //getSelected(product: Product): boolean {
  //  return product.name == this.selectedProduct;
  //}
}
