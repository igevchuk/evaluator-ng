import { Component, Inject } from "@angular/core";
import { ActivatedRoute } from "@angular/router";

import { Observable } from "rxjs/Observable";
import { Observer } from "rxjs/Observer";
import { State, Store } from "@ngrx/store";

@Component({
  selector: "blueprint-planning",
  templateUrl: "blueprint-planning.component.html"
})
export class BlueprintPlanningComponent {
}

// blueprint-planning.component
// blueprint-pricing.component
// blueprint-profile.component
// blueprint-settings.component
